1. Initialize a new NodeJS application
`npm init -y`

* If the folder has a `package.json` then it is a NodeJS application

2. Install packages
In python we have `pip`, therefore the same thing for JavaScript is npm
```
npm install @google/genai dotenv
```